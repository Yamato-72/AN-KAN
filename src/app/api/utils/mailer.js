// ============================================================
// 受注アラートメール（次回に再導入予定・現在は無効なスタブ）
//   - nodemailer をビルドに含めるには package-lock.json の更新が必要。
//     今日は 5段階化などの反映を優先し、メール送信はいったん無効化している。
//   - このファイルは何もせず false を返すだけ（外部依存なし＝ビルドは通る）。
//   - 次回、nodemailer を正しく導入したうえで、送信処理を実装し直す。
// ============================================================

export async function sendOrderAlert(/* project, actor */) {
  // メール機能は一時停止中。常に「送信しなかった」を返す。
  return false;
}

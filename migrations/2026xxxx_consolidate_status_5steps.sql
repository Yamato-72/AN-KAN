-- 7段階 → 5段階への集約に伴う、既存データの引っ越し
--   旧「国際発注済 / 設置手配済 / 設置完了」を「手配中」にまとめる。
--   「残金請求済」は従来どおり完了扱いなので変更しない。
--   リード / 打ち合わせ中 / 受注済み もそのまま。

UPDATE projects
SET status = '手配中', updated_at = NOW()
WHERE status IN ('国際発注済', '設置手配済', '設置完了');
